# Review — 2026-07-30 22:22 — 06-notes-rest-timer-inline
Diff: 55f0975^..1287cb3   Personas: Bug Hunter, Security, Architect

## Verdict: NEEDS_CHANGES

## Must-fix
- [Bug Hunter] `src/features/workout/InlineNoteField.tsx:41-45` (`commit()`) — `commit()` unconditionally calls `onSave(...)` on every blur, even when the draft is byte-identical to the value the field was seeded with (e.g. the user just tapped in to view a note and tapped away without typing anything) → in the routine editor this is a real, user-visible regression: `RoutineExerciseCard.tsx:189-190`'s `handleSaveNote` routes every `onSave` call through `mutate(...)` with no equality check, and `RoutineEditorScreen.tsx:208-211`'s `mutate` unconditionally calls `setDirty(true)`. `handleCancel` (`RoutineEditorScreen.tsx:220-228`) only skips the "Discard changes?" confirm when `!dirty`. Repro: open the routine editor, tap the now-always-visible note row just to look at it (previously a no-op — the old `NoteEditSheet` only fired `onSave` on an explicit "Save" button press), tap away to dismiss the keyboard, then press Cancel — the screen now falsely believes there are unsaved edits and shows "Discard changes? Your edits to this routine will be lost." even though nothing changed. This was verified directly by reading `RoutineEditorScreen.tsx:174,208-228` and `RoutineExerciseCard.tsx:189-190`.
  Fix direction: in `InlineNoteField.commit()`, compare the normalized draft against the normalized incoming `value` and skip calling `onSave` when nothing actually changed (e.g. `if (trimmed === (value ?? '')) { setEditing(false); return; }` before calling `onSave`).

## Notes (non-blocking)
- [Bug Hunter] `src/features/workout/InlineNoteField.tsx:43` — `onSave(trimmed.length > 0 ? draft : null)` persists the untrimmed `draft` (not `trimmed`) when non-empty, e.g. `"  Note  "` saves with the surrounding whitespace intact. This is inherited verbatim from the old `NoteEditSheet.handleSave` (not a regression introduced by this diff), but the PRD's own §4.1 prose ("commits the trimmed draft") doesn't match what the code actually does — worth a PRD wording fix if not a code fix, so a future reader doesn't assume trimming happens on save.

## Persona summaries
- **Bug Hunter — NEEDS_CHANGES.** One must-fix (above); one non-blocking pre-existing-behavior note.
- **Security — PASS.** Pure UI/interaction refactor; no new dependencies, no network calls, no persistence-semantics change, no new trust boundary. `onSave` call sites (`ExerciseCard.tsx:115-117`, `RoutineExerciseCard.tsx:189-190`) confirmed byte-identical to pre-diff (`updateExercise({notes})` / `updateDraftExerciseNotes(...)`, same args, only the caller/trigger changed). `NoteText.tsx` (the only component reaching `Linking.openURL`) is untouched and gated the same way it always was. `KeyboardDoneBar` only calls `Keyboard.dismiss()`. No findings.
- **Architect — PASS.** Diff matches PRD §4/§6 essentially file-for-file: `InlineNoteField.tsx`/`KeyboardDoneBar.tsx` new, `NoteEditSheet.tsx`+test deleted (repo-wide grep confirms zero remaining live references), `KeyboardDoneBar` correctly built as a separate `src/ui/` component rather than extending `KeyboardAccessoryBar`/`keyboardFocusStore` (matches the PRD's documented rejection rationale), `NOTES_KEYBOARD_ACCESSORY_VIEW_ID` colocated in `InlineNoteField.tsx` rather than added to the unrelated numeric registry, mount sites match exactly (`ActiveWorkoutScreen.tsx`, `EditWorkoutScreen.tsx`, `RoutineEditorScreen.tsx`'s first-ever accessory bar), `RoutineExerciseCard.tsx`/`RoutineExerciseMenuSheet.tsx` mirrored per the PRD's documented scope-expansion (§4.8), menu item counts match (7→6, 6→5 confirmed via `ExerciseCardMenuSheet.test.tsx`'s `onDismiss` count). No findings.

## Next step
Route back to `dev-code` with the Bug Hunter's must-fix item as its next task: add a no-op guard in `InlineNoteField.commit()` so blurring without an actual content change doesn't call `onSave` (and therefore doesn't mark the routine editor dirty / trigger a spurious "Discard changes?" prompt). Re-run `dev-review` on the follow-up diff once fixed — re-check specifically that `RoutineEditorScreen`'s `dirty` state stays `false` after opening and closing the note field with no edit, for both the "existing note, untouched" and "no note, opened and closed empty" cases.
