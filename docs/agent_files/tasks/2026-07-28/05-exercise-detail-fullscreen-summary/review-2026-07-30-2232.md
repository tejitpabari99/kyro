# Review — 2026-07-30 22:32 — 05-exercise-detail-fullscreen-summary

Diff: `564f367^..afa463f` (filtered to `app/**`, `src/**`; `docs/agent_files/tasks/**` noise from
interleaved sub-project history excluded)
Personas: Bug Hunter, Security, Architect

## Verdict: PASS

## Must-fix

None. All three personas returned PASS with zero must-fix findings.

## Notes (non-blocking)

- **[Bug Hunter]** `src/features/exercises/ExerciseDetailScreen.tsx:92` — the `workoutRepository`
  prop's doc comment still says "The Records tab does **not** depend on this prop at all." That's
  stale post-AD-5: Records' *visibility* is now gated behind `historicalSets.length === 0`, which
  *is* driven by `workoutRepository`/`historyQuery`. Harmless in production (the only call site,
  `app/exercise/[id].tsx`, always supplies it), but could mislead a future test author into writing
  a records-only test that silently renders the empty state instead of real content.
- **[Bug Hunter]** `src/features/exercises/__tests__/ExerciseDetailScreen.test.tsx` (~line 779,
  "shows the Summary tab by default...") — asserts `detail-summary` synchronously right after only
  waiting on `detail-name`, without a `waitFor` on `recordsQuery` also settling. Passed consistently
  in repeated local runs (RNTL's `act`/`waitFor` flushing happens to drain both queries' microtask
  chains together in this environment), but `historyQuery`/`recordsQuery` are logically independent
  async chains with no ordering guarantee — wrapping this assertion in `waitFor` (as a later test in
  the same file already does) would remove a theoretical race rather than relying on incidental
  timing.
- **[Security]** `src/features/routines/RoutineEditorScreen.tsx` / `ExercisePickerSheet.tsx:296` —
  data-scoping regression, not a security-boundary crossing: the routine editor's picker previously
  omitted `workoutRepository` from its nested detail sheet on purpose ("this editor already scopes
  exercise-detail access down"), so History/Charts/Records fell back to empty states there. After
  this diff, the ⓘ button routes to the real `/exercise/[id]` route, which always constructs a full
  `WorkoutRepositoryImpl` regardless of caller context — so a user in the routine editor now sees
  full workout history/PR data the old design deliberately withheld. Single-local-user app, no
  auth/multi-tenant boundary, so not a trust-boundary violation — flagged because the PRD's AD-13
  doesn't appear to explicitly call out this specific consequence when resolving "does the ⓘ-button
  fix need per-caller changes."
- **[Architect]** `src/features/exercises/ExerciseDetailScreen.tsx` (Summary branch) — the diff
  gates Summary-tab loading on `historyQuery.isLoading || recordsQuery.isLoading`, which AD-2's own
  code sample in the PRD didn't include (PRD only specified `historyQuery.isLoading`). This is a
  real, in-diff-documented bugfix (two independent async queries feeding one merged tab; the file's
  header comment explains the race and a dedicated regression test covers it) rather than
  unreviewed scope creep — flagged only as a literal deviation from the PRD's code sample, not as a
  problem.
- **[Architect]** AD-4's suspend/restore mechanism matches the PRD's proposed shape exactly (one
  `isNavigatingToDetail` boolean + one `useFocusEffect`, nothing extra grown on top); AD-8's
  dead-prop cleanup verified complete repo-wide via grep; AD-3's `HowToTab` extraction is
  byte-identical to the deleted inline `AboutTab`; AD-5's `ExerciseSummaryTab` is pure composition
  with no coupling introduced between its two children.

## Persona summaries

- **Bug Hunter (PASS):** Verified the `historyQuery.isLoading || recordsQuery.isLoading` double-gate
  is correct (recordsQuery is always `enabled`, so it can't stall the tab forever; "no historical
  sets implies no records" confirmed against the underlying SQL). Traced all four combinations of
  the `wasVisible` open-transition reset vs. the `useFocusEffect` restore in `ExercisePickerSheet`
  and found no race — the two are gated on independent signals (prop change vs. focus regain).
  Confirmed `Sheet.tsx` truly unmounts on `visible={false}` while the picker's own state lives above
  it in the parent, so suspend/restore genuinely preserves selection/search/filter state. Ran the
  affected test suites and `tsc --noEmit`; all pass.
- **Security (PASS):** Confirmed `exercise.id` interpolated into `router.push` is always either a
  CSPRNG UUIDv4 (custom exercises) or a fixed vendored-dataset slug (built-ins), never free-form
  user text — no injection/path-confusion surface. Repository-construction changes strictly reduce
  the number of live repository instances (no new singleton/credential/cross-session state). No
  secrets or credential-like strings in new/changed test fixtures.
- **Architect (PASS):** Diff matches all ten Architecture Decisions (AD-1 through AD-10). No
  over-engineering in the AD-4 suspend/restore mechanism; no incomplete dead-prop threading found
  via repo-wide grep; new/changed tests are proportionate to the risk the PRD itself flags (the
  first-ever `useFocusEffect` usage in the codebase gets dedicated, targeted coverage in the new
  `ExercisePickerSheet.test.tsx`).

## Next step

PASS — proceed to land / hand back to the human. The two non-blocking notes worth a cheap follow-up
if convenient (not required before merging): (1) update `ExerciseDetailScreen.tsx`'s stale
`workoutRepository` doc-comment sentence about Records not depending on it, and (2) consider whether
`RoutineEditorScreen`'s picker should keep its prior "no workout history/PR data" scoping now that
the ⓘ button routes through the shared, always-fully-wired `/exercise/[id]` route (flag as an Open
Question for a future PRD if the product intent was to preserve that restriction; not a blocker
today in a single-local-user app).
