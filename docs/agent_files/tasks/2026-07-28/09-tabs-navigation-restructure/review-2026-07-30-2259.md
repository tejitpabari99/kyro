# Review — 2026-07-30 22:59 — 09-tabs-navigation-restructure

Diff: `ed949b3^..6a42679` (filtered to `app/**` + `src/**`, 38 files, 1477 diff lines)
Personas: Bug Hunter, Security, Architect

## Verdict: PASS

All three reviewer personas independently returned PASS. No must-fix findings. The
highest-risk failure mode for this sub-project — dangling/stale route references
(`router.push`/`router.replace`/`href` strings still pointing at the old `/history/...`
or the removed `/exercises` tab route) after the `history`→`home` rename and Exercises-tab
removal — was specifically checked by all three personas (via full diff read + repo-wide
grep against the final tree at `6a42679`) and none found any executable call site left
unmigrated. All ~13 route-string call sites (`HistoryListScreen`, `CalendarScreen`,
`ExerciseHistoryTab`, `ExerciseRecordsTab`, `ProfileScreen`, `ActiveWorkoutScreen`,
`EditWorkoutScreen`, `HistoryDetailScreen`, plus the 6 `Slot→Stack` conversions and the
`app/index.tsx` redirect) are consistently updated.

## Must-fix

(none)

## Notes (non-blocking)

- [Bug Hunter, Architect — both flagged independently] `app/(tabs)/home/calendar.tsx:3`,
  `src/features/calendar/CalendarScreen.tsx:6` — header JSDoc still quotes the old call
  site `router.push('/history/calendar')`; the actual code now pushes `/home/calendar`.
  Comment-only, no runtime effect.
- [Architect] `app/(tabs)/home/[id].tsx:3` — header JSDoc still quotes
  `router.push(\`/history/${id}\`)` (actual call site now pushes `/home/${workoutId}`),
  and the same paragraph's rationale for why `app/exercise/[id].tsx` stays top-level
  ("because `(tabs)/exercises/` is a different, plural directory name") no longer parses
  since that directory was deleted by this diff.
- [Bug Hunter] Similar stale-comment drift also present in `app/(tabs)/profile/measures/index.tsx`,
  `app/(tabs)/profile/measures/photos/index.tsx`, `app/(tabs)/workout/index.tsx` (all
  reference the deleted `app/(tabs)/exercises/index.tsx` path in prose), and
  `app/__tests__/db-gate.test.tsx:614` ("HistoryRepository's list queries" — the actual
  dependency is `WorkoutRepositoryImpl`). `src/lib/share-file.ts:25` also references the
  pre-rename test path `app/(tabs)/history/__tests__/history.test.tsx`, but that file is
  untouched by this diff (pre-existing, out of scope for this sub-project).
- [Bug Hunter] `app/(tabs)/profile/settings/index.tsx`, `src/features/stats/StatisticsScreen.tsx`
  — the new back-chevron sits inside the scrollable content region (whole screen is one
  `<ScrollView>`) rather than a fixed header above a nested `ScrollView`, unlike
  `CalendarScreen`/`MeasuresHomeScreen`/`ExerciseBrowseScreen`. This matches each screen's
  pre-existing structure (not introduced by this diff) and native swipe-back
  (`gestureEnabled: true`) remains available regardless of scroll position — not a
  functional break, just a minor visual-consistency observation across the 6 back-button
  screens.
- [Architect] Verified the PRD's literal `swipeDirection: 'vertical'` prop name (§4.4)
  was intentionally shipped as `gestureDirection: 'vertical'` instead, with an inline code
  comment in `app/_layout.tsx` explaining why: `swipeDirection` only exists on the
  underlying native `ScreenProps`, not on expo-router's `NativeStackNavigationOptions`
  type used at the `<Stack.Screen options>` layer, so the PRD's literal prop name would
  fail `tsc`. Confirmed via `node_modules/expo-router`'s vendored native-stack type
  declarations that `gestureDirection: 'vertical'` bundles the same
  `fullScreenGestureEnabled: true` + `animationMatchesGesture: true` +
  `animation: 'slide_from_bottom'` defaults the PRD's research describes for
  `swipeDirection`. This is a correct, well-documented technical correction of the PRD's
  prop-name research, not a deviation in behavior — no finding.

## Next step

PASS → proceed to land / hand back to the human (e.g. `ship`).
