# Review — 2026-07-30 22:15 — 04-exercise-picker-settings-icon

Diff: `170458e^..a7be016`   Personas: Bug Hunter, Security, Architect

## Verdict: PASS

All three personas returned PASS with zero must-fix findings. The diff adds a gear
("List options") icon to `ExercisePickerSheet`'s header (both `mode="add"` and
`mode="replace"`), which opens a new `ExercisePickerOptionsSheet` with two rows —
"Reset Filters" (clears local `equipmentFilter`/`muscleFilter` state) and "More Settings"
(closes the picker, then `router.push('/profile/settings')`). The header was migrated
from hand-rolled `View` markup to the shared `SheetHeader` primitive (per the reconciled
TASKS.md, using its `right: { kind: 'custom', ... }` escape hatch), superseding the PRD's
original hand-rolled-header JSX while preserving the same specified visual outcome (title
left, gear icon + Cancel grouped right, gear left of Cancel).

## Must-fix

(none)

## Notes (non-blocking)

- [Bug Hunter] `src/features/workout/__tests__/ExerciseCard.operations.test.tsx` — the
  "Reset Filters row is disabled... onPress gated off" test never actually fires a press
  to prove gating; it only asserts `accessibilityState.disabled` (the test's own comment
  explains RNTL's `fireEvent.press` doesn't honor `disabled`). Test name overclaims
  slightly relative to what it verifies — self-documented, not a production-code issue.
- [Bug Hunter] `src/features/workout/ExercisePickerSheet.tsx` — the gear `Pressable`
  (`hitSlop={8}`) sits `spacing['3']` from the `Cancel` button; if that spacing constant
  is smaller than 16px, the two elements' expanded hit regions could touch. UX/layout
  concern, not a correctness bug — worth a look by whoever owns visual/design review.
- [Architect] The inline gear `Pressable`/`SettingsIcon` combo in the `SheetHeader`
  `right` custom slot isn't factored into a shared component, but it mirrors the same
  inline pattern already used for the equivalent icon in
  `src/features/profile/ProfileScreen.tsx:249-257` (same `hitSlop`, size-minus-2,
  `strokeWidth`, color). No `IconButton` primitive exists in `src/ui/` to reuse instead,
  so this isn't a missed-reuse opportunity.
- [Security] `router.push('/profile/settings')` is a hardcoded, non-parameterized static
  route string — no path-injection or open-redirect surface. No new dependency, no new
  persisted state, no logging of anything sensitive; matches PRD §5 "API Change Summary:
  None."
- [Architect] `handleOpenAppSettings`'s ordering (`setOptionsSheetVisible(false)` →
  `onDismiss()` → `router.push(...)`) matches PRD §4.3's sequencing rationale exactly
  (component-level `Sheet` must not stay mounted under navigation) — correctly
  implemented, not just documented in the PRD.
- [Architect] `SheetHeader`'s `right: { kind: 'custom', content }` slot is used exactly
  as that primitive's own doc comment intends — idiomatic use of the escape hatch, not a
  workaround signaling the primitive is under-built for this case.

## Persona verdicts

- Bug Hunter: PASS — verified `handleResetFilters`/`handleOpenAppSettings` semantics,
  `filtersActive` derivation, `optionsSheetVisible` correctly excluded from the
  `wasVisible` reset block (consistent with `equipmentSheetVisible`/`muscleSheetVisible`
  precedent), and all three real call sites of `ExercisePickerSheet`'s `onDismiss`
  (`EditWorkoutScreen.tsx`, `ActiveWorkoutScreen.tsx`, `RoutineEditorScreen.tsx`) are
  synchronous, side-effect-free — so the "close-then-navigate" ordering is safe.
- Security: PASS — no injection, authz, secret-leakage, or attack-surface-widening
  concerns; all inputs are static strings or fixed-enum local state, no new
  network/persistence.
- Architect: PASS — diff correctly implements the TASKS.md-reconciled architecture
  (real `SheetHeader` primitive, not the PRD's original hand-rolled markup), preserves
  PRD §4.2's specified visual outcome, follows existing codebase conventions (dumb
  presentational nested-sheet component, controlled-callback shape matching
  `FilterOptionSheet`), and correctly implements the no-mode-gating resolution (§9.4).

## Next step

PASS → proceed to land / hand back to the human (e.g. `ship`).
